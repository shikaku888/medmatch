# Demo Video Script (EN) — App Store Review
Length: ~30 sec | Device: iPhone 15 Pro (US region)

1. Open app → "MedMatch" (EN default) → banner FDA visible at bottom.
2. Tap "Scan Barcode / OCR" → camera activates (AVFoundation native) → scan a supplement barcode.
3. Result: interaction list shown; severity labeled clearly; any CYP-inferred pair has yellow label "Not clinician-verified".
4. Tap "Check" → deterministic verdict + evidence line (FDA label / SUPP.AI DOI) — no LLM-added facts.
5. Tap chat → ask about scanned product; answer comes from engine (not open-ended medical Q&A).
6. Tap "Print / Save PDF" → report has disclaimer + source citations.
7. Tap "Subscription" → StoreKit 2 screen: Pro $19/mo, Caregiver $69/mo, Restore button visible.
8. End with footer disclaimer: "Reference information only — not medical advice." No claims of treatment/diagnosis.
