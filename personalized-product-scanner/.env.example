# MedMatch AI backend URL (FastAPI 7-layer engine). Default: http://127.0.0.1:8765
MEDMATCH_URL="http://127.0.0.1:8765"

# No API keys required — OCR is on-device Tesseract.js, the advisor is a
# deterministic rule-based synthesizer, and swaps come from the iDISK catalog.
