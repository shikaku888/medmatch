# Databases (regenerable via python -m backend.db + importers)
medmatch/backend/medmatch.db
medmatch/backend/medmatch.db-shm
medmatch/backend/medmatch.db-wal

# Python
__pycache__/
*.pyc
.pytest_cache/

# Node
node_modules/
dist/
server.js

# Env / secrets
.env
.env.local

# OS / editor
Thumbs.db
.DS_Store

# Tesseract runtime language cache
*.traineddata

# SQLite lookup cache (runtime)
lookup_cache.db
lookup_cache.db-shm
lookup_cache.db-wal

# NC-licensed research backups (never distribute)
medmatch/backend/data/_nc_backup/

# DSLD bulk download (public domain, ~800MB — user-supplied, not distributed)
medmatch/backend/data/dsld/
# Local user data and generated test artifacts
personalized-product-scanner/data_storage.json
.pytest-*/
.locale-smoke-devices/
*.db
*.db-*
*.db.*

# Local archives and development certificates
*.tar
*.tar.gz
*.rar
*.pem

# Generated frontend bundles outside the canonical static tree
personalized-product-scanner/dist-*/
personalized-product-scanner/aisuckhoemedmatchstaticscanner/
medmatch/-b
